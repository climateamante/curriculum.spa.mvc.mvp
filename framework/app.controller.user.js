define(function(){

    // Instantiates the base class for controller.user.

    App.prototype.controller.user = {};

    return{
      app_controller_user : App.prototype.controller.user
    };

});
